
#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ПрограммныйИнтерфейс

// Фасад адаптера источника данных цифрового двойника.
// Реализация stub живёт в модуле объекта; при появлении TOS
// сюда добавляется выбор конкретной реализации.

Функция ПолучитьСнимок() Экспорт

	ОбработкаОбъект = Создать();
	Возврат ОбработкаОбъект.ПолучитьСнимок();

КонецФункции

Функция ПолучитьЧанкСнимка(SnapshotId, ChunkIndex) Экспорт

	ОбработкаОбъект = Создать();
	Возврат ОбработкаОбъект.ПолучитьЧанкСнимка(SnapshotId, ChunkIndex);

КонецФункции

Функция ПолучитьИзменения(ПослеВерсии) Экспорт

	ОбработкаОбъект = Создать();
	Возврат ОбработкаОбъект.ПолучитьИзменения(ПослеВерсии);

КонецФункции

Функция ПолучитьСостояниеОбмена() Экспорт

	ОбработкаОбъект = Создать();
	Возврат ОбработкаОбъект.ПолучитьСостояниеОбмена();

КонецФункции

#КонецОбласти

#КонецЕсли
