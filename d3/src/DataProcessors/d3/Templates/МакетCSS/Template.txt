<style>
html, body {
    width: 100%;
    height: 100%;
    margin: 0;
    padding: 0;
    overflow: hidden;
    background: #1a1d24;
    font-family: Segoe UI, Tahoma, sans-serif;
}

#stage {
    position: absolute;
    top: 0;
    right: 0;
    bottom: 0;
    left: 0;
    width: 100%;
    height: 100%;
    overflow: hidden;
    background: linear-gradient(180deg, #2a3140 0%, #1a1d24 100%);
}

#viewport {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
}

#viewport canvas {
    display: block;
    width: 100%;
    height: 100%;
    outline: none;
    cursor: grab;
}

#viewport.looking canvas {
    cursor: grabbing;
}

#hud {
    position: absolute;
    top: 12px;
    left: 12px;
    z-index: 2;
    max-width: calc(100% - 120px);
    pointer-events: none;
}

#help,
#status {
    font-size: 12px;
    color: #e8ecf2;
    background: rgba(20, 24, 32, 0.72);
    border: 1px solid rgba(255, 255, 255, 0.12);
    border-radius: 4px;
    padding: 6px 10px;
    margin-bottom: 6px;
}

#controls {
    position: absolute;
    top: 12px;
    right: 12px;
    z-index: 2;
}

#controls button {
    height: 32px;
    padding: 0 12px;
    margin-left: 6px;
    border: 1px solid #6b778c;
    background: #2c3444;
    color: #e8ecf2;
    font-size: 13px;
    cursor: pointer;
    border-radius: 4px;
}

#controls button:first-child {
    margin-left: 0;
}

#controls button:hover {
    background: #3a4558;
    border-color: #8fa0b8;
}

#controls button.active {
    background: #4a5d3a;
    border-color: #8fbf6a;
}

#diag-panel {
    position: absolute;
    left: 12px;
    right: 12px;
    bottom: 12px;
    z-index: 4;
    max-height: 42%;
    overflow: auto;
    margin: 0;
    padding: 10px 12px;
    font-size: 11px;
    line-height: 1.45;
    white-space: pre-wrap;
    word-break: break-word;
    color: #d7e0ec;
    background: rgba(10, 12, 18, 0.92);
    border: 1px solid rgba(255, 255, 255, 0.16);
    border-radius: 4px;
    pointer-events: auto;
}

#webgl-error {
    position: absolute;
    inset: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    color: #f5d0d0;
    font-size: 16px;
    background: #1a1d24;
    z-index: 3;
}

/* Ловит клавиши в WebKit ПолеHTMLДокумента (canvas сам их часто не получает) */
#key-trap {
    position: absolute;
    left: 0;
    top: 0;
    width: 1px;
    height: 1px;
    margin: 0;
    padding: 0;
    border: 0;
    opacity: 0;
    z-index: 5;
    outline: none;
    background: transparent;
    color: transparent;
    caret-color: transparent;
}
</style>